# Frontend

Landing page + live mutation report viewer for the Mutanchor demo site.

Sourced separately. Requirements:

- Static build, deployable to Vercel, single URL (landing + report viewer)
- Light mode
- No mock data: report panel loads the real generated report from the CLI
- No video hero, no sliders

Drop the frontend source here when ready.
