# Rust
/target
**/target
**/*.rs.bk

# Env
.env
.env.*
!.env.example

# Local-only demo script (never pushed)
DEMO_SCRIPT.md

# Node
node_modules
dist
.next
out

# Build artifacts / reports (generated, never committed).
# EXCEPT: the published dashboard payload at frontend/public/report.json,
# which IS committed — it's what /dashboard renders after `scripts/publish-report.sh`.
report.json
!frontend/public/report.json
report-full.json
dashboard.json
report.html

# Misc
.DS_Store
*.log
